# This is where installed files will be placed ready for deployment to the
# Libera IOC.  The IOC files will be placed in $INSTALL_HOST/tmbf.
#INSTALL_HOST=/home/libera/nfs/testing
INSTALL_HOST=/home/libera/nfs/testing

VERSION=1.0.6

# Extra delay on DAC output to align bunches.  This needs to be different for
# different TMBF machines
X_IOC_DACDLY=621
Y_IOC_DACDLY=620
T_IOC_DACDLY=620

# Parameters for filter.  The filter is canonically constructed from N cycles
# of a sine wave fitted into M points.  For X we use 2/9, for Y 3/8.
X_IOC_FIR_CYCLES=2
X_IOC_FIR_LENGTH=9
Y_IOC_FIR_CYCLES=3
Y_IOC_FIR_LENGTH=8
T_IOC_FIR_CYCLES=2
T_IOC_FIR_LENGTH=9

# Internal skew between input and output of DDC in ns.
IOC_DDC_SKEW=32
T_IOC_DDC_SKEW=-231
